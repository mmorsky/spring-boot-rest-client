spring:
  main:
    banner-mode: 'off'
    allow-bean-definition-overriding: true
  rest:
    client:
      services:
        localhost: http://localhost
      retry:
        max-attempts: 2

localhost:
  uri: http://localhost